package starters;

import engine.Client;
import engine.SuperGameEngine;

public class TestStarter {
	public static void main(String[] args) {
		//new Thread(() -> new GameEngine().startGame(1));
		//new Thread(() -> new Client().startGame("kage"));
	}
}
